---
import type { HTMLAttributes } from 'astro/types';

export type ButtonVariant = 'primary' | 'secondary' | 'ghost' | 'inverse' | 'accent' | 'outline';
export type ButtonSize = 'sm' | 'md' | 'lg';

type Props = {
  variant?: ButtonVariant;
  size?: ButtonSize;
  href?: string;
  class?: string;
} & Omit<HTMLAttributes<'a'>, 'class'> &
  Omit<HTMLAttributes<'button'>, 'class'>;

const { variant = 'primary', size = 'md', href, class: className, ...rest } = Astro.props;

const Tag = href ? 'a' : 'button';

const variants: Record<ButtonVariant, string> = {
  primary: 'bg-brand-600 text-neutral-0 hover:bg-brand-700 border-transparent',
  secondary: 'bg-transparent text-ink-brand border-brand-600 hover:bg-brand-50',
  ghost: 'bg-transparent text-current border-transparent hover:bg-current/10',
  inverse: 'bg-neutral-0 text-ink-brand border-transparent hover:bg-brand-50',
  accent: 'bg-accent-600 text-neutral-0 hover:bg-accent-500 border-transparent',
  outline: 'bg-transparent text-ink-inverse border-neutral-0 hover:bg-neutral-0/10',
};

// min-h keeps every size above the 44px touch target on small screens.
const sizes: Record<ButtonSize, string> = {
  sm: 'min-h-11 px-4 text-sm',
  md: 'min-h-12 px-6 text-base',
  lg: 'min-h-14 px-8 text-lg',
};
---

<Tag
  href={href}
  class:list={[
    'inline-flex items-center justify-center gap-2 rounded-none border font-medium tracking-wide uppercase',
    'no-underline transition-colors duration-150',
    'disabled:pointer-events-none disabled:opacity-50',
    variants[variant],
    sizes[size],
    className,
  ]}
  {...rest}
>
  <slot />
</Tag>
